import React, { useState } from 'react'; // Ensure useState is imported
import './ImageResize.css';

const ImageResize = () => {
    // Define your state and other logic here
    const [image, setImage] = useState(null);

    return (
        <div>
            <p> hiiii</p>
        </div>
    );
};

export default ImageResize;
